/**
 * Global test setup file for Vitest.
 * Imports @testing-library/jest-dom which adds extra "matchers" to expect(),
 * such as: .toBeInTheDocument(), .toHaveTextContent(), .toBeVisible(), etc.
 * This runs once before every test file.
 */
import "@testing-library/jest-dom";
